server:
  port: 8080
spring:
  application:
    name: heyformuser
  profiles:
    active: prod
  cloud:
    config:
      uri: http://localhost:8888
management:
  endpoints:
    web:
      exposure:
        include: refresh