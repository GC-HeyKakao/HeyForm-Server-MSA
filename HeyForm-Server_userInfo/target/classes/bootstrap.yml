server:
  port: 8080
spring:
  application:
    name: heyform
  profiles:
    active: prod
  cloud:
    config:
      uri: http://localhost:8888
management:
  endpoints:
    web:
      exposure:
        include: refresh